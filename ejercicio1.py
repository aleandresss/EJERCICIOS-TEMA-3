


class pila:
    def __init__ (self):
        self.agujas = []

def cantidad(self): #funcion para preguntad por la cantidad de datos en la cola 
        return len(self.agujas)

def desapilar_agujas (self):
        if len(self.agujas)>0:
            self.agujas.pop(0) #con el cero; empieza a extraer desde arriba// sin el cero desde abajo 
            return self.agujas
        
def ordenar_agujas (self, )



def insertar_agujas (self,aguja):
        self.agujas.append(ficha)
        return self.agujas

